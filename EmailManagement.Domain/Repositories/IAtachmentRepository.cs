namespace EmailManagement.Infrastructure.Repositories
{
    public interface IAtachmentRepository
    {
        //Task SaveAsync(Atachment atachment);
        //Task<Atachment?> GetByIdAsync(Guid id);
        //Task UpdateAsync(Atachment atachment);
        //Task DeleteAsync(Atachment atachment);
        // Outros métodos, se necessário
    }
}
